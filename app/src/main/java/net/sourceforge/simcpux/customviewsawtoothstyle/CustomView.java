package net.sourceforge.simcpux.customviewsawtoothstyle;import android.content.Context;import android.graphics.Canvas;import android.util.AttributeSet;import android.util.Log;import android.view.View;/** * Created by shaomiao on 2016-12-26. */public class CustomView extends View {    private static final String TAG = "CustomView";    private static final int mRectCount =6;    public CustomView(Context context) {        super(context);    }    public CustomView(Context context, AttributeSet attrs) {        super(context, attrs);    }    public CustomView(Context context, AttributeSet attrs, int defStyleAttr) {        super(context, attrs, defStyleAttr);    }    @Override    protected void onMeasure(int widthMeasureSpec, int heightMeasureSpec) {        int specMode = MeasureSpec.getMode(widthMeasureSpec);        int specSize = MeasureSpec.getSize(widthMeasureSpec);        Log.e(TAG, "onMeasure: specMode" +specMode);        Log.e(TAG, "onMeasure: specSize" + specSize);        int Exactly =  MeasureSpec.EXACTLY; // 精确模式        Log.e(TAG, "onMeasure: Exactly"+Exactly);        int AT_MOST = MeasureSpec.AT_MOST; // 最大值模式        Log.e(TAG, "onMeasure: AT_MOST"+AT_MOST);        int UNSPECIFIED = MeasureSpec.UNSPECIFIED; // 不指定模式        Log.e(TAG, "onMeasure: UNSPECIFIED"+UNSPECIFIED);        setMeasuredDimension(widthMeasureSpec, heightMeasureSpec);    }    @Override    protected void onDraw(Canvas canvas) {        super.onDraw(canvas);        for (int i = 0;i< mRectCount; i++) {        }        // View重绘        invalidate();        // 延迟重绘        postInvalidateDelayed(300);    }}